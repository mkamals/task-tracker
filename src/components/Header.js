import '../App.css';
const Header = () => {
    return (
        <header className="header">
        <h3>Task Tracker</h3>
        <small>This is a simple task tracker to add and list the tasks</small>
        </header>
    )
}

export default Header